<UserControl x:Class="UserEventDemo.ShowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserEventDemo"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <Label Content="Contact Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <Label Content="Name:" FontWeight="Bold"/>
            <Label Name="NameLabel" Margin="0,0,0,10" Content="{Binding Name}"/>

            <Label Content="Phone Number:" FontWeight="Bold"/>
            <Label Name="PhoneNumberLabel" Margin="0,0,0,10" Content="{Binding PhoneNumber}"/>

            <Label Content="Email:" FontWeight="Bold"/>
            <Label Name="EmailLabel" Margin="0,0,0,10" Content="{Binding Email}"/>

        </StackPanel>
    </Grid>
</UserControl>

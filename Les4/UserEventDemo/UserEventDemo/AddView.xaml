<UserControl x:Class="UserEventDemo.AddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserEventDemo"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Label Content="Name:"/>
            <TextBox x:Name="NameTextBox" Text="{Binding Name}"/>
            <Label Content="Tel:"/>
            <TextBox x:Name="TelTextBox" Text="{Binding PhoneNumber}"/>
            <Label Content="Email:"/>
            <TextBox x:Name="EmailTextBox" Text="{Binding Email}"/>

            <Button Click="Button_Click">Save</Button>
            <Button>Cancel</Button>

        </StackPanel>
    </Grid>
</UserControl>
